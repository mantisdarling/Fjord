{
  "name": "fjord",
  "version": "0.1.0",
  "private": true,
  "description": "Private browser activity intelligence dashboard and Chrome extension foundation",
  "scripts": {
    "check": "node scripts/check.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
